% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{mp_power_checkpoint}
\alias{mp_power_checkpoint}
\title{Resumable, checkpointed power simulation}
\usage{
mp_power_checkpoint(
  scenario,
  nsim,
  file,
  batch_size = 100,
  alpha = 0.05,
  seed = NULL,
  failure_policy = c("count_as_nondetect", "exclude"),
  conf_level = 0.95,
  ci_method = c("clopper-pearson", "wald"),
  progress = TRUE
)
}
\arguments{
\item{scenario}{An \code{mp_scenario}.}

\item{nsim}{Total number of simulations desired.}

\item{file}{Path to the checkpoint \code{.rds} file.}

\item{batch_size}{Replicates per batch (default 100).}

\item{alpha, seed, failure_policy, conf_level, ci_method}{As in \code{\link[=mp_power]{mp_power()}}.
\code{seed} must be non-NULL for reproducible, resumable batches.}

\item{progress}{Emit a progress message after each batch (default \code{TRUE}).}
}
\value{
An object of class \code{mp_power} for all \code{nsim} replicates.
}
\description{
Runs \code{\link[=mp_power]{mp_power()}} in batches, saving the accumulated per-replicate results to
\code{file} (an \code{.rds}) after each batch. If \code{file} already exists for the same
\code{seed}, the run resumes from where it left off and only the remaining
replicates are simulated. This makes very large or long-running power
analyses robust to interruption, and lets you grow \code{nsim} later without
recomputing finished replicates.
}
\details{
Because replicate seeds are deterministic (\code{seed + i - 1}), the result is
identical to a single \code{mp_power(scenario, nsim, seed = seed)} call: batching
only changes when work happens, not what is computed.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  d <- mp_design(list(subject = 30), trials_per_cell = 6)
  a <- mp_assumptions(list("(Intercept)" = 0, condition = 0.4),
                      random_effects = list(subject = list(intercept_sd = 0.5)),
                      residual_sd = 1)
  scn <- mp_scenario_lme4(y ~ condition + (1 | subject), design = d, assumptions = a)
  f <- tempfile(fileext = ".rds")
  mp_power_checkpoint(scn, nsim = 60, file = f, batch_size = 20, seed = 1)
}
}
}
\seealso{
\code{\link[=mp_power]{mp_power()}}.
}
