% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from-fit.R
\name{mp_from_fit}
\alias{mp_from_fit}
\title{Build a power scenario from a fitted lme4 model}
\usage{
mp_from_fit(
  fit,
  test_term = NULL,
  test_method = NULL,
  null_formula = NULL,
  pb_nsim = 100L,
  extend = NULL
)
}
\arguments{
\item{fit}{A fitted model of class \code{lmerMod}/\code{lmerModLmerTest} (Gaussian LMM)
or \code{glmerMod} (binomial/Poisson/negative-binomial GLMM).}

\item{test_term}{Fixed-effect term to test. Defaults to the first
non-intercept fixed effect.}

\item{test_method}{Inference method. Gaussian fits allow \code{"wald"} (default),
\code{"satterthwaite"}, \code{"kenward-roger"}, \code{"lrt"}, \code{"pb"}; GLMM fits allow
\code{"wald"}, \code{"lrt"}, \code{"pb"}.}

\item{null_formula}{Null-model formula required for \code{"lrt"}/\code{"pb"}. Defaults
to the fitted formula with \code{test_term} removed.}

\item{pb_nsim}{Bootstrap replicates for \code{test_method = "pb"} (default 100).}

\item{extend}{Optional named list of target level counts per grouping factor
(e.g. \code{list(Subject = 60)}) used to scale the pilot's sample size up or
down. Levels are cloned from the pilot's structure with fresh ids and fresh
random effects drawn from the fitted covariance. See \code{\link[=mp_extend]{mp_extend()}} and the
\verb{extend.<group>} sensitivity key for power curves over N.}
}
\value{
An object of class \code{mp_scenario}.
}
\description{
Turns an existing \code{lmer}/\code{glmer} fit (e.g. from pilot or published data) into
an \code{mp_scenario}, so its estimated effects and variance components inform a
simulation-based power analysis. New responses are simulated from the fitted
model with \code{\link[stats:simulate]{stats::simulate()}} (keeping the estimated random-effect structure
and residual variance), the model is refit, and the focal term is tested.
}
\details{
The fixed effects used to simulate are read from the scenario's assumptions,
which start at the fitted coefficients. This means \code{mp_sensitivity()} /
\code{mp_power_curve()} can vary an effect size (e.g.
\code{fixed_effects.condition}) for data-based vs smallest-effect-of-interest
comparisons. Sample size can be scaled up or down from the pilot with
\code{\link[=mp_extend]{mp_extend()}} (or the \verb{extend.<group>} sensitivity key), which clones the
pilot's structure with fresh levels and fresh random effects.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  m <- lme4::lmer(Reaction ~ Days + (Days | Subject), data = lme4::sleepstudy)
  scn <- mp_from_fit(m, test_term = "Days")
  mp_power(scn, nsim = 20, seed = 1)
}
}
}
