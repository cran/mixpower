% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve-sample-size.R
\name{mp_grid_sample_size}
\alias{mp_grid_sample_size}
\title{Create a grid of values for sample-size search}
\usage{
mp_grid_sample_size(from, to, length.out = NULL, by = NULL)
}
\arguments{
\item{from}{Lower bound (inclusive).}

\item{to}{Upper bound (inclusive).}

\item{length.out}{Number of points (optional). Uses \code{\link[=seq]{seq()}} with
\code{length.out}; \code{from} and \code{to} are the first and last values.}

\item{by}{Step size (optional). Uses \code{\link[=seq]{seq()}} with \code{by}; sequence runs from
\code{from} to \code{to} in steps of \code{by}.}
}
\value{
A numeric vector. For integer cluster sizes, use
\code{round(mp_grid_sample_size(...))} or pass \code{by} as an integer (e.g. \code{by = 10}).
}
\description{
Returns a numeric vector suitable for \code{\link[=mp_solve_sample_size]{mp_solve_sample_size()}}'s \code{grid}
argument. Specify either the number of points (\code{length.out}) or the step
size (\code{by}); bounds are always explicit (\code{from}, \code{to}).
}
\examples{
mp_grid_sample_size(20, 100, length.out = 9)
mp_grid_sample_size(20, 100, by = 10)
}
