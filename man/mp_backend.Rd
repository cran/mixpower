% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-contract.R
\name{mp_backend}
\alias{mp_backend}
\title{MixPower backend contract}
\usage{
mp_backend(
  simulate_fun,
  fit_fun,
  test_fun,
  name = "custom",
  version = NULL,
  notes = NULL,
  capabilities = NULL
)
}
\arguments{
\item{simulate_fun}{A data.frame-generating simulator (see Details).}

\item{fit_fun}{Model fitter (see Details).}

\item{test_fun}{Extracts a scalar p-value (see Details).}

\item{name}{Short label for printing and manifests (default \code{"custom"}).}

\item{version}{Optional character version string for the backend implementation.}

\item{notes}{Optional longer notes.}

\item{capabilities}{Optional named list of flags (documentation only), e.g.
\code{list(families = c("gaussian"), supports_lrt = TRUE)}.}
}
\value{
An object of class \code{c("mp_backend", "list")} with the components above.
}
\description{
A \strong{backend} is a list with three functions used by \code{\link[=mp_power]{mp_power()}}:
\describe{
\item{\code{simulate_fun}}{First argument receives the \code{mp_scenario} (often named
\code{scenario}); optional \code{seed}. Must return a \code{data.frame}.}
\item{\code{fit_fun}}{Two arguments: simulated \code{data.frame}, then the \code{mp_scenario}
(names may differ; \code{\link[=mp_power]{mp_power()}} passes them positionally).}
\item{\code{test_fun}}{Two arguments: fitted model, then the \code{mp_scenario}; returns
\verb{list(p_value = <numeric scalar>)}.}
}
}
\details{
Use \code{mp_backend()} to build a validated object of class \code{mp_backend}. Custom
backends can also be plain lists with those three names; \code{validate_mp_backend()}
checks the contract without requiring the class.
}
