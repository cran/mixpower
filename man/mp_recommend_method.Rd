% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{mp_recommend_method}
\alias{mp_recommend_method}
\title{Recommend an inference method for a scenario}
\usage{
mp_recommend_method(scenario, small_clusters = 30L)
}
\arguments{
\item{scenario}{An \code{mp_scenario}.}

\item{small_clusters}{Threshold below which the smallest grouping factor is
treated as "few clusters" (default 30).}
}
\value{
An object of class \code{mp_recommendation}: a list with \code{method} (the
scenario's current method), \code{n_groups} (smallest grouping-factor size),
\code{is_lmm}, \code{caution} (logical), \code{recommended} (character vector), and
\code{rationale}.
}
\description{
Heuristic guidance on the \code{test_method} for a scenario, based on the number of
levels of the random grouping factors. Wald (normal-approximation z/t) tests
and, to a lesser extent, likelihood-ratio tests are known to be
anti-conservative when the number of clusters is small (Luke, 2017): the
degrees of freedom are overstated, so the test rejects too often. With few
clusters, a degrees-of-freedom-corrected test (Satterthwaite or Kenward-Roger,
for linear mixed models) or a parametric bootstrap (any family) controls Type
I error far better.
}
\details{
This is a fast, design-based heuristic; to \emph{measure} a specific design and
method, use \code{\link[=mp_calibrate]{mp_calibrate()}}.
}
\examples{
d <- mp_design(list(subject = 12), trials_per_cell = 8)
a <- mp_assumptions(
  fixed_effects = list("(Intercept)" = 0, condition = 0.4),
  random_effects = list(subject = list(intercept_sd = 0.5)),
  residual_sd = 1
)
scn <- mp_scenario_lme4(y ~ condition + (1 | subject), design = d, assumptions = a)
mp_recommend_method(scn)
}
\seealso{
\code{\link[=mp_calibrate]{mp_calibrate()}}.
}
