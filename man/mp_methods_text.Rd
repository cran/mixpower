% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-text.R
\name{mp_methods_text}
\alias{mp_methods_text}
\title{Generate a methods paragraph for a power analysis}
\usage{
mp_methods_text(result, software = TRUE)
}
\arguments{
\item{result}{An \code{mp_power} object (from \code{\link[=mp_power]{mp_power()}}).}

\item{software}{Include a sentence naming the mixpower package and version
(default \code{TRUE}).}
}
\value{
A length-1 character string with class \code{mp_methods_text} (its
\code{print()} method word-wraps the paragraph).
}
\description{
Produces a ready-to-edit prose description of a simulation-based power
analysis from an \code{\link[=mp_power]{mp_power()}} result: the design, model, effect tested,
inference method, number of simulations, and the estimated power with its
interval. Intended to seed the "Power analysis" paragraph of a methods
section.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  d <- mp_design(list(subject = 30), trials_per_cell = 6)
  a <- mp_assumptions(list("(Intercept)" = 0, condition = 0.4),
                      random_effects = list(subject = list(intercept_sd = 0.5)),
                      residual_sd = 1)
  scn <- mp_scenario_lme4(y ~ condition + (1 | subject), design = d, assumptions = a)
  mp_methods_text(mp_power(scn, nsim = 50, seed = 1))
}
}
}
