% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend.R
\name{mp_extend}
\alias{mp_extend}
\title{Scale a fitted-model scenario's sample size up or down}
\usage{
mp_extend(scenario, ...)
}
\arguments{
\item{scenario}{An \code{mp_scenario} created by \code{\link[=mp_from_fit]{mp_from_fit()}}.}

\item{...}{Named target level counts, e.g. \code{Subject = 60}.}
}
\value{
The scenario with its \code{extend} targets set.
}
\description{
Sets target level counts for one or more grouping factors of a scenario built
with \code{\link[=mp_from_fit]{mp_from_fit()}}, so power can be evaluated at a sample size different
from the pilot's. This is the analogue of \code{simr::extend()}: levels are cloned
from the pilot's within-level structure with fresh ids, and fresh random
effects are drawn from the fitted covariance, so the extended data represent a
larger (or smaller) sample from the same population.
}
\details{
Pair with \code{\link[=mp_power]{mp_power()}} for a single N, or with \code{\link[=mp_power_curve]{mp_power_curve()}} /
\code{\link[=mp_solve_sample_size]{mp_solve_sample_size()}} using the \verb{extend.<group>} key for a curve over N.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  m <- lme4::lmer(Reaction ~ Days + (Days | Subject), data = lme4::sleepstudy)
  scn <- mp_from_fit(m, test_term = "Days")
  big <- mp_extend(scn, Subject = 40)
  mp_power(big, nsim = 20, seed = 1)
}
}
}
\seealso{
\code{\link[=mp_from_fit]{mp_from_fit()}}.
}
