% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_parallel.R
\name{mp_sensitivity_parallel}
\alias{mp_sensitivity_parallel}
\title{Parallel sensitivity analysis over a parameter grid}
\usage{
mp_sensitivity_parallel(
  scenario,
  vary,
  nsim = 100,
  alpha = 0.05,
  seed = NULL,
  failure_policy = c("count_as_nondetect", "exclude"),
  conf_level = 0.95,
  workers = 2L,
  progress = FALSE,
  checkpoint_dir = NULL,
  resume = TRUE,
  ...
)
}
\arguments{
\item{scenario}{A base \code{mp_scenario} object.}

\item{vary}{Named list of vectors. Names are dotted paths such as
\code{"fixed_effects.condition"} or \code{"clusters.subject"}.}

\item{nsim}{Number of simulations for each grid cell.}

\item{alpha}{Significance threshold.}

\item{seed}{Optional seed for reproducible cell-wise execution.}

\item{failure_policy}{Failure policy passed to \code{\link[=mp_power]{mp_power()}}.}

\item{conf_level}{Confidence level passed to \code{\link[=mp_power]{mp_power()}}.}

\item{workers}{Number of parallel workers when \code{progress = FALSE} (default 2).}

\item{progress}{If \code{TRUE}, run serially with a text progress bar.}

\item{checkpoint_dir}{Optional directory to save per-cell RDS results and a
manifest. When \code{resume = TRUE}, existing cell files are reused if the manifest
matches the current run. Use a path on \strong{shared storage} if \code{workers > 1}.}

\item{resume}{Logical; only used when \code{checkpoint_dir} is set.}

\item{...}{Reserved.}
}
\value{
An object of class \code{mp_sensitivity} (same structure as \code{\link[=mp_sensitivity]{mp_sensitivity()}}).
}
\description{
Like \code{\link[=mp_sensitivity]{mp_sensitivity()}}, but evaluates each grid cell in parallel (or with a
progress bar when \code{progress = TRUE}). Uses per-cell seeds \code{seed + cell_index - 1L}
to match a serial ordering convention. Does not modify \code{\link[=mp_power]{mp_power()}}.
}
\note{
Parallel execution requires the \pkg{parallel} package and that \pkg{mixpower}
can be loaded on workers (installed package).
}
