% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sesoi.R
\name{mp_safeguard_effect}
\alias{mp_safeguard_effect}
\title{Safeguard (confidence-bound) effect size from a fitted model}
\usage{
mp_safeguard_effect(fit, term = NULL, conf_level = 0.9)
}
\arguments{
\item{fit}{A fitted \code{lmer}/\code{glmer} model.}

\item{term}{Fixed-effect term. Defaults to the first non-intercept effect.}

\item{conf_level}{Two-sided confidence level for the interval (default
\code{0.90}). Lower values are less conservative.}
}
\value{
An object of class \code{mp_safeguard}: a list with \code{term}, \code{estimate},
\code{se}, \code{conf_level}, the interval (\code{lower}, \code{upper}), and the \code{safeguard}
bound (the interval limit nearest zero, in the direction of the estimate).
}
\description{
Computes a \emph{safeguard} effect for power analysis: the bound of a confidence
interval for a fitted effect that lies closest to zero (Perugini, Gallucci &
Costantini, 2014). Planning power around this conservative, uncertainty-aware
value protects against the optimism of using a noisy pilot point estimate.
}
\details{
The interval is the Wald (normal-approximation) interval from the fitted
coefficient and its standard error. Pair the result with \code{\link[=mp_from_fit]{mp_from_fit()}} and
\code{\link[=mp_sesoi]{mp_sesoi()}} to run a safeguard-power simulation.
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  m <- lme4::lmer(Reaction ~ Days + (Days | Subject), data = lme4::sleepstudy)
  sg <- mp_safeguard_effect(m, term = "Days", conf_level = 0.90)
  sg
}
}
}
\seealso{
\code{\link[=mp_sesoi]{mp_sesoi()}}, \code{\link[=mp_from_fit]{mp_from_fit()}}.
}
