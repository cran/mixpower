% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick-power.R
\name{mp_quick_power}
\alias{mp_quick_power}
\title{Quick power run for a single LMM design}
\usage{
mp_quick_power(
  formula,
  clusters,
  trials_per_cell = 1,
  fixed_effects,
  residual_sd,
  nsim,
  alpha = 0.05,
  seed = NULL,
  random_effects = NULL,
  icc = NULL,
  test_method = c("wald", "lrt"),
  null_formula = NULL,
  predictor = "condition",
  subject = "subject",
  outcome = "y",
  item = NULL,
  ...
)
}
\arguments{
\item{formula}{Model formula (e.g. \code{y ~ condition + (1 | subject)}).}

\item{clusters}{Named list of cluster sizes (e.g. \code{list(subject = 40)}).}

\item{trials_per_cell}{Number of observations per cell (default 1).}

\item{fixed_effects}{Named list of effect sizes (e.g. \code{list(condition = 0.3)}).
Include intercept as \code{(Intercept)} if needed.}

\item{residual_sd}{Residual standard deviation.}

\item{nsim}{Number of simulations.}

\item{alpha}{Significance level (default 0.05).}

\item{seed}{Optional seed for reproducibility.}

\item{random_effects}{Optional named list of random-effect sizes, e.g.
\code{list(subject = list(intercept_sd = 0.5))}. See \code{\link[=mp_assumptions]{mp_assumptions()}}.}

\item{icc}{Deprecated; interpreted as a random-intercept SD. Use
\code{random_effects}.}

\item{test_method}{\code{"wald"} (default) or \code{"lrt"}.}

\item{null_formula}{Required when \code{test_method = "lrt"} (e.g. \code{y ~ 1 + (1 | subject)}).}

\item{predictor}{Predictor column name (default \code{"condition"}).}

\item{subject}{Subject ID column name (default \code{"subject"}).}

\item{outcome}{Outcome column name (default \code{"y"}).}

\item{item}{Optional item ID column name.}

\item{...}{Arguments passed to \code{\link[=mp_power]{mp_power()}} (e.g. \code{failure_policy}, \code{conf_level}, \code{keep}).}
}
\value{
The result of \code{\link[=mp_power]{mp_power()}} (object of class \code{mp_power}).
}
\description{
One-call wrapper that builds design, assumptions, scenario (lme4 LMM), and
runs \code{\link[=mp_power]{mp_power()}}. Intended for the common case: one fixed effect, one
random intercept. All arguments are explicit; pass further options to
\code{\link[=mp_power]{mp_power()}} via \code{...} (e.g. \code{failure_policy}, \code{conf_level}, \code{keep}).
}
\examples{
mp_quick_power(
  y ~ condition + (1 | subject),
  clusters = list(subject = 40),
  trials_per_cell = 8,
  fixed_effects = list(`(Intercept)` = 0, condition = 0.3),
  residual_sd = 1,
  nsim = 50,
  seed = 123
)
}
