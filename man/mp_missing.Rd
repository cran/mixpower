% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{mp_missing}
\alias{mp_missing}
\title{Add a missing-data / dropout mechanism to a scenario}
\usage{
mp_missing(
  scenario,
  mechanism = c("mcar", "mar", "dropout"),
  prob = NULL,
  on = NULL,
  slope = 0,
  time = NULL,
  dropout = NULL,
  subject = "subject"
)
}
\arguments{
\item{scenario}{An \code{mp_scenario} (any backend).}

\item{mechanism}{One of \code{"mcar"}, \code{"mar"}, \code{"dropout"}.}

\item{prob}{Baseline deletion probability for \code{"mcar"}/\code{"mar"}.}

\item{on}{Name of the observed column the \code{"mar"} probability depends on.}

\item{slope}{Logit-scale slope for \code{"mar"} (default 0).}

\item{time}{Name of the within-subject ordering column for \code{"dropout"}.}

\item{dropout}{For \code{"dropout"}: a numeric vector of cumulative dropout
proportions per ordered timepoint, or \code{list(shape=, scale=)} for Weibull.}

\item{subject}{Subject grouping column (default \code{"subject"}).}
}
\value{
The scenario with its simulator wrapped to apply missingness.
}
\description{
Wraps a scenario so that, on every replicate, observations are deleted from
the simulated data before the model is fit. This lets a power analysis
reflect realistic incomplete data (Gallop & Liu, 2017; Magnusson, 2018).
Three mechanisms are supported:
}
\details{
\itemize{
\item \code{"mcar"}: each observation is deleted independently with probability
\code{prob} (missing completely at random).
\item \code{"mar"}: each observation is deleted with a probability that depends on an
\emph{observed} column \code{on} through a logistic model
\code{plogis(qlogis(prob) + slope * on)} (missing at random).
\item \code{"dropout"}: monotone longitudinal dropout along \code{time} within each
subject --- once a subject drops out it contributes no later observations.
The dropout pattern is given either by \code{dropout} as a vector of cumulative
dropout proportions (one per ordered timepoint) or as
\code{list(shape =, scale =)} for a Weibull dropout time on the \code{time} scale.
}
}
\examples{
\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  d <- mp_design(list(subject = 30), trials_per_cell = 6,
                 predictors = list(time = "continuous"))
  a <- mp_assumptions(list("(Intercept)" = 0, time = 0.4),
                      random_effects = list(subject = list(intercept_sd = 0.5)),
                      residual_sd = 1)
  scn <- mp_scenario_lme4(y ~ time + (1 | subject), design = d,
                          assumptions = a, predictor = "time")
  scn_drop <- mp_missing(scn, "dropout", time = "time",
                         dropout = c(0, 0.1, 0.2, 0.35, 0.5, 0.6))
  mp_power(scn_drop, nsim = 20, seed = 1)
}
}
}
